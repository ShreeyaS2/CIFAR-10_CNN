## CNN Based on CIFAR-10
This is an entry-level CNN(convolutional neural network) consisting of two layers made for the famous CIFAR-10 dataset, for the purpose of image classification. The predictions are compared to the images along with their labels.
